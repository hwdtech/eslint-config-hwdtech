# @hwdtech/eslint-config

## Installation

```bash
npx install-peerdeps --dev @hwdtech/eslint-config # installs required dependencies for eslint configuration
npm install --save-dev @hwdtech/eslint-config # installs the config itself
```
